 k get pods --selector env=prod,bu=finance,tier=frontend ====== this will select the pods that meets the following labels.
 
 k get pods --selector env=prod,bu=finance,tier=frontend --no-headers | wc -l =======this will output the number of items. (wc means word count)
